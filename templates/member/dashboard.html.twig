{% extends 'site/base.html.twig' %}

{% block title %}Espace membre{% endblock %}

{% block body %}

    <div class="row my-5">
        {# colonne menu #}
        <div class="col-12 col-md-3 col-lg-2 bg-secondary">
            {% include "member/components/_menu.html.twig" %}
        </div>
        {# colonne des cards #}
        <div class="col-12 col-md-9 col-lg-10 d-flex flex-wrap p-3">
            <div class="col-12">
                {# table paiements #}
                {% include "member/components/_card_paiements.html.twig" %}
            </div>
            <div class="col-12">
                {# table questions #}
                {% include "member/components/_card_questions.html.twig" %}
            </div>
        </div>
    </div>

{% endblock %}